{
  "dependencies": {
    "gitbook-cli": "^2.3.2"
  },
  "scripts": {
  "preinstall": "npm install graceful-fs@4 --save && npm dedupe",
  "build": "npx gitbook install && npx gitbook build ./ ./datalabperu"
}

}
